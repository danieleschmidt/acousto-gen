{
  "apiVersion": "apps/v1",
  "kind": "Deployment",
  "metadata": {
    "name": "acousto-gen",
    "namespace": "acousto-gen-dev",
    "labels": {
      "app": "acousto-gen",
      "environment": "development"
    }
  },
  "spec": {
    "replicas": 1,
    "selector": {
      "matchLabels": {
        "app": "acousto-gen"
      }
    },
    "template": {
      "metadata": {
        "labels": {
          "app": "acousto-gen",
          "environment": "development"
        }
      },
      "spec": {
        "containers": [
          {
            "name": "acousto-gen",
            "image": "your-registry.com/acousto-gen:latest",
            "ports": [
              {
                "containerPort": 8000
              }
            ],
            "env": [
              {
                "name": "ENV",
                "value": "development"
              },
              {
                "name": "DEBUG",
                "value": "true"
              },
              {
                "name": "LOG_LEVEL",
                "value": "DEBUG"
              }
            ],
            "resources": {
              "limits": {
                "cpu": "500m",
                "memory": "1Gi"
              },
              "requests": {
                "cpu": "500m",
                "memory": "1Gi"
              }
            },
            "livenessProbe": {
              "httpGet": {
                "path": "/health",
                "port": 8000
              },
              "initialDelaySeconds": 60,
              "periodSeconds": 30
            },
            "readinessProbe": {
              "httpGet": {
                "path": "/ready",
                "port": 8000
              },
              "initialDelaySeconds": 10,
              "periodSeconds": 5
            }
          }
        ]
      }
    }
  }
}